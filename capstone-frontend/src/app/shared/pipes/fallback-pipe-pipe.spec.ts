import { FallbackPipePipe } from './fallback-pipe-pipe';

describe('FallbackPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FallbackPipePipe();
    expect(pipe).toBeTruthy();
  });
});
